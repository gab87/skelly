import type { Meta, StoryObj } from '@storybook/react';
import { SkellyCard } from '../src/components/SkellyCard/SkellyCard';

const meta: Meta<typeof SkellyCard> = {
  title: 'Components/SkellyCard',
  component: SkellyCard,
  argTypes: {
    animation: {
      control: 'select',
      options: ['none', 'shiny', 'flicker'],
    },
    speed: { control: 'number' },
    background: { control: 'color' },
    color: { control: 'color' },
    border: { control: 'text' },
  },
  decorators: [
    (Story) => (
      <div style={{ width: 300 }}>
        <Story />
      </div>
    ),
  ],
};

export default meta;
type Story = StoryObj<typeof SkellyCard>;

export const Default: Story = {};

export const WithShiny: Story = {
  args: {
    animation: 'shiny',
    speed: 1500,
  },
};

export const WithFlicker: Story = {
  args: {
    animation: 'flicker',
    speed: 2000,
  },
};

export const CustomColor: Story = {
  args: {
    color: '#caffbf',
    border: '1px solid #80ed99',
  },
};
